publc_interface Electronic {
boolean off;
boolean on;
}
publc class Computer implement Electronic{
    public boolean on() {
        reeturn false;
    }
    @override
    public boolean off();
}
boolean onOrOff= false;

boolean on();

defuat void electronic(){
    if(onOrOff){
        system.out.printin("default");
    }
}
//an example
public class processor {

    string type = corei5;
    string generation = "8th gen";
}
public class RAM{
    string name = DDR4;
    string size = 16GB;
}
public class storage{
    sting type = "SSD";
    string manufacturer = "samsung";
    int capacity = 512;
}
RAM ram;

Processor cpu;

Storage storage;
public Computer(RAM ram, Processor cpu, Storage storage){
    this.ram = ram;
    this.cpu = cpu;
    this.storage = storage;
}
public class Admin extends User {

    static void addNummer(int a, int b){
        system.out.printin(a+b);
    }
    public static void main(string[] args){
        addNumber(a=4, b=5)
    }
    string staffId;
    void uploadResults(string course, float score){
        scanner AdminInput = new Scanner(system.in);
        system.out.printIn(course + scoree); 
    }
    public static void main(string[] args){
        Admin staffId = new Admin();
        staffId.password = "New staff";
        system.out.printIn(staffId.name)
    }
    Computer {
        new Computer(new RAM(), new Processor(), new storage())
    }
}
public String toString(){
    return computer{
        ram = ram;
        processor = cpu;
        storage = storage;
    }
    return(ram(), processor(), storage())
}
